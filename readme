# Sistema de Notas Universitario - Version 1.0

## Descripcion del proyecto
Aplicacion web que permite gestionar notas de estudiantes universitarios. Desarrollada con Python usando Flask como framework backend (incluye servidor WSGI Werkzeug), SQLite como base de datos, y HTML/CSS/JavaScript 
para el frontend. Permite crear estudiantes con cedula y nombre, agregar notas del 0 al 10, quitar la ultima nota y calcular el promedio automaticamente. 
La base de datos se guarda en un archivo local llamado "estudiantes.db" que persiste entre ejecuciones.

## Estructura del proyecto

mi_universidad/
├── app.py                  # Backend con Flask y SQLite
├── templates/
│   └── index.html          # Frontend con HTML/CSS/JS
└── estudiantes.db          # Base de datos (se crea automaticamente)


## Como abrir y ejecutar

1. Abrir terminal o CMD
2. Navegar a la carpeta del proyecto:
   cd ruta/donde/esta/mi_universidad
   (Ejemplo en este repositorio: `cd mi_universidad`)
3. Instalar Flask (solo primera vez):
   pip install flask
4. Ejecutar la aplicacion:
   python app.py
5. Abrir navegador en: http://localhost:5000
6. Para cerrar: presionar Ctrl + C en la terminal

## Funcionalidades

- Crear estudiante: ingresar cedula y nombre
- Agregar nota: ingresar cedula y nota (0-10)
- Quitar nota: elimina la ultima nota del estudiante
- Ver promedio: muestra el promedio de todas las notas del estudiante

## Autor
KEVIN ANDRES GARCES ECHEVERRI
kevingarces315956@correo.itm.edu.co


